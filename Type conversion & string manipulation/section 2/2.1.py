#Calculate area of circle (input as string → convert to float)

radius_str=input("Enter a radius of circle:-")
radius=float(radius_str)

area=3.14*(radius**2)
print("the area of circle is:-",area)
